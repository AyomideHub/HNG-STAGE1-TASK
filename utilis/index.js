const { numProperties} = require('./armstrong')
const {digitSum} = require('./digit_sum')
const {isPerfect} = require('./perfectnum')
const {isPrime} = require('./PrimeNumber')


module.exports = {numProperties, digitSum, isPerfect, isPrime}